package redbacks.arachne.core.references;

/**
 * RobotMap SHOULD hold all of the magic numbers the robot ever uses.
 * This makes it easier to make slight tweaks to values in the code, or rearrange the ports that actuators and sensors are wired into.
 */
public class RobotMap {
	//Global variables
	public static boolean
		isAutoController = true;
}